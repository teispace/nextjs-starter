#!/usr/bin/env sh

echo '🔎 Type checking before push...'

yarn type-check

if [ $? -ne 0 ]; then
    echo '❌ Type check failed. Fix the errors before pushing.'
    echo '💡 CI will run the full pipeline (lint, tests, build) on push.'
    exit 1
fi

echo '✅ Type check passed. Pushing...'
