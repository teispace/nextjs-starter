nodeLinker: node-modules

# Keep Yarn's supply-chain quarantine on (default 1d) for every package: refuse
# versions younger than the gate, so a malicious publish usually gets yanked
# before we install it.
npmMinimalAgeGate: 1d
